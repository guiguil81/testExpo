import React from 'react';
import { Text, SafeAreaView } from 'react-native';

const Menu = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Text>Menu</Text>
    </SafeAreaView>
  );
};

export default Menu;
